# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.*

# Generated critique-loop artifacts (screenshots + JSON) — future training
# data, kept locally, not committed (would bloat the repo with binaries).
training-data/
# Resumable critic-benchmark cache (keyed by critic version + model + input).
critic-cache.json
# Semantic fallback-model certification record (keyed by critic version + model).
fallback-cert.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
