export interface GridConfig {
  gridWidth: number;
  gridHeight: number;
  spacing: number;
}

export interface FieldParams {
  depthScale: number;
  springStiffness: number;
  damping: number;
  animationSpeed: number;
}
